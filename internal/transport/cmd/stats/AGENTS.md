# Stats subcommand

The `mocode stats` CLI renders session statistics. Static HTML/CSS/JS assets
live alongside the Go command in this directory and are served by the stats
command implementation.

See [AGENTS.md](../../../AGENTS.md) for build and test conventions.
