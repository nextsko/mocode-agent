// Package integration holds external integrations: the WeChat bot and butler
// (with its long-running gateway entry and SDK) and OAuth login handlers.
// These depend on core, domain, and util; they are entry surfaces alongside
// transport and ui.
package integration
