// Package domain is a container for pure domain models and DTOs (sessions,
// messages, shared types, prompt history, file tracking). Sub-packages hold
// no I/O; they depend only on util primitives. See internal/README.md.
package domain
