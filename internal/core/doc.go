// Package core is the application core: agent runtime and orchestration
// (agents, tools, coordinator, candidate selection, failover), composition
// root (app), config, permissions, hooks, skills, knowledge, evaluation,
// evolution, and the shell execution engine. Core depends on domain, store,
// and util, and is consumed by the transport, integration, and ui layers.
package core
